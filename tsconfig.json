{
  "compilerOptions": {
    "target": "es6",                          // Compila para ES6, compatível com Node.js
    "module": "commonjs",                      // Usado para importações no estilo Node.js
    "outDir": "./dist",                        // Diretório de saída dos arquivos compilados
    "rootDir": "./src",                        // Diretório raiz dos arquivos TypeScript
    "strict": true,                            // Ativa todas as verificações de tipo rigorosas
    "esModuleInterop": true,                   // Permite compatibilidade com módulos CommonJS e ES6
    "skipLibCheck": true,                      // Ignora checagem de tipo em declarações de biblioteca para reduzir o tempo de build
    "forceConsistentCasingInFileNames": true   // Garante que nomes de arquivos tenham o mesmo casing em importações
  },
  "include": [
    "src"                                      // Inclui a pasta "src" para compilar
  ],
  "exclude": [
    "node_modules",                            // Exclui a pasta "node_modules" da compilação
    "dist"                                     // Exclui a pasta "dist" da compilação
  ]
}
